const nodemailer = require("nodemailer");
const Mailgen = require("mailgen");

// Nodemailer Transport
const transporter = nodemailer.createTransport({
  host: process.env.SMTP_HOST, // e.g., "smtp.gmail.com"
  port: process.env.SMTP_PORT, // 465 (SSL) or 587 (TLS)
  secure: process.env.SMTP_SECURE === "true", // convert string to boolean
  auth: {
    user: process.env.SMTP_USER,
    pass: process.env.SMTP_PASS,
  },
});

// Mailgen Setup
const mailGenerator = new Mailgen({
  theme: "default",
  product: {
    name: "Software Engineering - FUTO",
    link: "https://futo.edu.ng/department/soe",
    logo: "https://futo.edu.ng/logo.png",
  },
});

exports.sendEmailToMultiple = async ({ emails, notice }) => {
  try {
    // Email body generated by Mailgen
    console.log("Notice details:", notice);

    const emailBody = {
      body: {
        name: "Dear Student",
        intro:
          "We’ve got a new notice for you released by Software Engineering Departmment.",
        table: {
          data: [
            { Title: notice.title },
            { Date: new Date().toDateString() },
            { "Ref Id": notice.refId },
            { Description: notice.description },
          ],
        },
        action: {
          instructions: "Click the button below to view the notice:",
          button: {
            color: "#272362",
            text: "View Notice",
            link: `hhttp://localhost:8000/api/notice/:${notice.id}`,
          },
        },
        outro:
          "For any questions, reach us at contact@futo.edu.ng_department_soe",
      },
    };

    const emailTemplate = mailGenerator.generate(emailBody);

    const message = {
      from: `"Software Engineering - FUTO" <${process.env.SMTP_USER}>`,
      to: emails, // array of emails
      subject:
        "New Notice Released on Software Engineering Department Notice Board",
      html: emailTemplate,
    };

    const info = await transporter.sendMail(message);
    console.log("Emails sent successfully:", info.messageId);
    return info;
  } catch (error) {
    console.error("Error sending email:", error);
    throw error;
  }
};
